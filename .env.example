# --- Google Cloud Platform ---
# El ID del proyecto de Google Cloud donde corre BigQuery y GCS
PROJECT_ID=tu-proyecto-gcp-id

# El nombre del bucket de GCS donde se guardarán los datos RAW (Bronze)
RAW_BUCKET=tu-bucket-raw-name

# Ruta a las credenciales de servicio JSON (opcional si usas gcloud auth o Cloud Run)
# GOOGLE_APPLICATION_CREDENTIALS=./credentials/service-account.json

# --- Supabase (Origen de Datos) ---
# URL de la API de tu proyecto Supabase (https://xyz.supabase.co)
SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave de servicio de Supabase (SERVICE_ROLE_KEY o ANON_KEY)
# Nota: Para extracción masiva sin RLS, se recomienda SERVICE_ROLE_KEY.
SUPABASE_SERVICE_KEY=tu-supabase-service-role-key

# --- Configuración Opcional ---
# Nivel de logs (INFO, DEBUG, WARNING)
LOG_LEVEL=INFO
